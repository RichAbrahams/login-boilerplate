// import { fromJS } from 'immutable';
// import { makeSelectSendResetDomain } from '../selectors';

// const selector = makeSelectSendResetDomain();

describe('makeSelectSendResetDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
