// import { fromJS } from 'immutable';
// import { makeSelectSignOutDomain } from '../selectors';

// const selector = makeSelectSignOutDomain();

describe('makeSelectSignOutDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
